export default () => {
  return {
    limit (config) {
      return config
    },
    offset (config) {
      return config
    },
    flatten (config) {
      return config
    },
    execute (config) {
      return config
    }
  }
}
